/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'picsum.photos', 
        'loremflickr.com', 
        'cloudflare-ipfs.com', 
        'avatars.githubusercontent.com'
      ],
    },
  };
  
  export default nextConfig